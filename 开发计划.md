# 分布式 Agent 服务开发计划

> 本文是 [`开发规划.md`](./开发规划.md) 的执行态：规划回答"做什么、为什么这么做"，
> 本计划回答"谁做、什么时候做、做完怎么算通过"。
> 两份文档的编号可互相索引：任务卡的 `依据` 列指向规划的 A–H 节，`不变量` 列指向 F 节。

**团队**：3 人后端（R1 分布式/Redis · R2 通信层/协议 · R3 Agent/工具/消息）
**工作量**：72.5 人日 ≈ 14.5 人周（与规划估算一致）
**日历周期**：9 周（W1–W9），关键路径 P0 → P1 → P3 → P5
**最高风险**：P3（3.6 人周，占总量 25%，且四条不变量全部落在这一阶段）

---

## 0. 计划前提

### 0.1 沿用规划已锁的决策

规划 G 节的 25 条技术决策、C 节的键命名、F 节的 12 条不变量，**在本计划内视为冻结输入，不再重开讨论**。
任何一条要改，走 §7 的变更流程，而不是在任务卡里悄悄改。

### 0.2 两个悬置项按推荐值先行假设

规划里还有两处没定死。计划按推荐值排期，并在 W1 的决策会上确认；若否决，按下表调整。

| 悬置项 | 本计划假设 | 否决后的影响 |
|---|---|---|
| workspace 文件系统（规划 A 节） | **方案 B**：`RemoteFilesystemSpec` + `BaseStore`(Redis)，`IsolationScope.USER` | 改方案 A（关长期记忆）→ P0 减 0.5 人日，失去跨 session 记忆沉淀 |
| 部署形态（规划 D 节） | **P1–P4 合并部署**，单进程双 profile；P5 后按连接规模评估拆分 | 无代码影响，只改启动参数（这正是 D 节设计的收益） |

### 0.3 计划本身的两条约束

- **P0 的产出全部是"上线后改不了"的约定**（键名、分片数、msgSeq、协议 v1）。P0 不验收就不允许开 P1，
  否则后续每一阶段都在往一个会变的地基上砌墙。
- **P2 与 P3 并行时，两条轨道不允许改同一个包**。分工见 §8.2，这是并行排期成立的前提。

---

## 1. 排期总览

```
     W1      W2      W3      W4      W5      W6      W7      W8      W9
R1  ├P0──┤├──── P1 ────┤├────────── P3 ──────────┤├── P5 ──────┤├─P6─┤
R2  ├P0──┤├──── P1 ────┤        ├──── P3 ────┤├P4─┤├── P5 ─────┤├─P6─┤
R3  ├P0──┤├──── P1 ────┤├── P2 ──────┤├∥Skill┤├──── 前端联调 ──┤├─P6─┤
         ▲            ▲            ▲            ▲       ▲      ▲     ▲
        G0           G1           G2           G3      G4     G5    G6
```

| 周 | 主线 | 并行轨道 | 周末门禁 |
|---|---|---|---|
| W1 | **P0 地基**（全员） | 云 Redis 实测（R1，W1 周一必须启动） | **G0** |
| W2 | P1 单节点闭环（全员） | | |
| W3 | P1 收尾与验收 | R3 转入 P2 | **G1** |
| W4 | **P3 前半**（R1：摘牌 Lua / 消费组 / 心跳 / 回收）+ 混沌台搭建 | P2 消息渲染（R3） | |
| W5 | **P3 主体**（R1+R2） | P2 验收 → **G2** | |
| W6 | P3 收尾 + 混沌验收 | ∥ Skill 仓库（R3，0.5 人周） | **G3** |
| W7 | P5 前半（R1：ctrl Lua / 指令分类） | P4 断线重连（R2，1 人周）+ 客户端三规则联调（R3） | **G4**（P4） |
| W8 | P5 收尾与验收（R1+R2+R3） | | **G5** |
| W9 | P6 容量与降级（全员）+ 压测 | | **G6** |

**压缩到 8 周的唯一方式**：W7 让 R2 的 P4 与 R1 的 P5 完全并行（本计划已如此安排），
再把 P6 的模型降级（P6-5）挪到上线后迭代。**不要压缩 P3** —— 那里省下的每一天都会在混沌测试里还回来。

---

## 2. 工程结构

Maven 多模块。模块边界与规划 D 节的模块划分一一对应，**模块间不允许直接依赖对方的实现类**。

```
java-agent-sdk/
├── agent-protocol/     # 客户端协议 v1、ClientMessage、DTO、错误码。无 Spring 依赖
├── agent-keys/         # 键命名规范的唯一实现：KeyNamespace + 256 分片。无业务依赖
├── agent-store/        # Redis 脚本封装、消息表 Repository、msgSeq 分配、冷存储写入
├── agent-agent/        # HarnessAgent 单例装配、工具、middleware、render()
├── agent-comm/         # 收消息模块 + 发消息模块（WebFlux Controller / SSE）
├── agent-task/         # 任务控制模块 + Worker 模块
├── agent-bootstrap/    # 启动装配，profile: comm | task | all（拆分部署只改这里的参数）
└── agent-it/           # testcontainers 集成测试 + 混沌测试 + 压测脚本
```

**依赖方向（单向，不允许反向或横向）**：

```
agent-comm ─┐
            ├─→ agent-store ─→ agent-keys
agent-task ─┤                     ↑
            └─→ agent-agent ──────┘
所有模块 ─→ agent-protocol
```

**架构守护测试**（P0 交付，ArchUnit）：
- `agent-comm` 不得引用 `agent-task` 的任何类，反之亦然 —— 对应规划 D 节"模块间没有任何直接调用"
- 任何模块拼接 Redis key 必须经过 `agent-keys.KeyNamespace`，禁止字符串字面量 —— 守 INV-9
- `agent-agent` 的 middleware 包内出现阻塞调用签名时告警 —— 守 INV-7

这三条不是洁癖。规划 D 节说得很清楚：一旦出现模块间直接调用，独立部署和独立扩容就没了。
守护测试是唯一能让这条约束在半年后还成立的手段。

---

## 3. 任务分解（WBS）

估算单位为**人日**（1 人周 = 5 人日）。`依据` 指向 `开发规划.md` 的节，`不变量` 指向 F 节编号。

### P0　地基　5 人日 · W1 · 全员

| ID | 任务 | 交付物 | 人日 | 负责 | 前置 | 依据 | 不变量 |
|---|---|---|---|---|---|---|---|
| P0-1 | **云 Redis 实测**：确认 7.0 引擎、标准版实例、`EVAL/EVALSHA` 开放；**实测脚本内 `XADD ... *`** | 一页实测报告（含 ctrl 脚本实跑截图） | 1.0 | R1 | — | A §阿里云选型 | — |
| P0-2 | workspace 方案决策会 + 落地（`RemoteFilesystemSpec` + Redis `BaseStore`，`IsolationScope.USER`） | 决策记录 + 可运行配置 | 0.5 | R3 | — | A §workspace | — |
| P0-3 | 工程骨架：8 个 Maven 模块、CI（编译/单测/覆盖率门禁）、ArchUnit 守护测试 | 可 `mvn verify` 的空壳工程 | 1.0 | R2 | — | §2 | INV-7/9 |
| P0-4 | `KeyNamespace`：256 分片 hash tag、7 类 key 的构造方法、**分片数以常量+注释锁死** | `agent-keys` 模块 + 单测 | 0.5 | R1 | P0-3 | C | INV-9 |
| P0-5 | HarnessAgent 单例 Bean + `RedisDistributedStore`（启动构建一次） | `agent-agent` 可跑单轮对话 | 1.0 | R3 | P0-2 | E §P0 | — |
| P0-6 | 全线统一 Lettuce reactive（禁止 Jedis 进入依赖树，`maven-enforcer` 拦） | 依赖收敛 + enforcer 规则 | 0.25 | R1 | P0-3 | G | — |
| P0-7 | 消息表 DDL + **`msgSeq` 分配方案**（per-session Redis `INCR`，禁用全局自增） | DDL 脚本 + 分配器 + 单测 | 0.75 | R2 | P0-3 | B §消息序号 | **INV-10** |
| P0-8 | 客户端协议 v1：`type` 枚举、`fallbackText` 必填、能力协商、**客户端重试约定成文** | `agent-protocol` + 协议文档 | 1.0 | R2 | — | B/C/INV-1 | **INV-1** |

**G0 门禁**：单进程完成一次多轮对话；**进程重启后相同 `(userId, sessionId)` 能接着聊**（框架自动恢复，不写恢复代码）；
P0-1 实测报告出结论；协议 v1 冻结并同步给前端。

> P0-1 必须 W1 周一开工。它是唯一可能推翻 P3 整套设计的外部依赖——若 Lua 被禁，
> P3 改走"每 60s 扫 inbox 非空且无 lease 的 session 重新入队"的兜底方案（最坏延迟 90s），
> 估算从 18 人日降到 11 人日，但恢复时延从秒级退化到分钟级。**这个结论越早拿到越好。**

---

### P1　单节点闭环　12 人日 · W2–W3 · 全员

| ID | 任务 | 交付物 | 人日 | 负责 | 前置 | 依据 | 不变量 |
|---|---|---|---|---|---|---|---|
| P1-1 | `POST /sessions/{sid}/messages` → `202 {replyId, msgSeq}`，不等 agent | Controller + 鉴权 + 参数校验 | 1.5 | R2 | P0-8 | E §P1 | — |
| P1-2 | 投递写入：`XADD inbox` → `XADD ready`（**此阶段即按最终顺序写**） | InboxWriter | 1.0 | R2 | P0-4 | C | **INV-1** |
| P1-3 | ready 简易消费循环 + lease 抢占（`SET NX PX`，值每次唯一） | 任务控制模块 v0 | 1.5 | R1 | P0-4 | E §P1 | **INV-3** |
| P1-4 | Worker：session 初始化 → drain inbox → `streamEvents(msg, ctx)` 接入 | Worker 骨架 | 2.0 | R1 | P1-3, P0-5 | D | INV-4 |
| P1-5 | **OutboxWriter**：`bufferTimeout(64, 80ms)` → 合并相邻同 blockId delta → **先落消息表 → 后 `XADD`** | OutboxWriter + 单测 | 2.5 | R3 | P0-7 | B/E §P1 | **INV-5 / INV-8** |
| P1-6 | `GET /sessions/{sid}/events` SSE，`id = msgSeq`，**建连不解析任何位置参数，全量重放窗口** | 发消息模块 v1 | 2.0 | R2 | P1-5 | B §重连协议 | INV-10 |
| P1-7 | 事件日志异步落冷存储（`doOnNext` 旁路，失败不影响 turn） | 冷存储 Writer | 0.5 | R3 | P1-4 | B | **INV-5** |
| P1-8 | 端到端联调 + 流式流畅度实测（10–20 次/秒） | 联调报告 | 1.0 | 全员 | 全部 | E §P1 | — |

**明确不做**：多节点、断线降级、控制通道、卡片。全部留给后续阶段——这是规划 E 节的原话，照办。

**G1 门禁**：单用户完整对话，文本流式无卡顿；**刷新页面能续上**；
`concatMap` 用于事件管道、`flatMap` 只出现在 ready 消费（代码评审逐处确认，守 INV-8）。

---

### P2　消息渲染　10 人日 · W4–W5 · R3 主导

| ID | 任务 | 交付物 | 人日 | 负责 | 前置 | 依据 | 不变量 |
|---|---|---|---|---|---|---|---|
| P2-1 | `ClientMessage` 协议 + `render(toolName, payload)` **纯函数**（无 IO、可单测） | render 实现 + 表驱动单测 | 2.0 | R3 | P0-8 | E §P2 | — |
| P2-2 | `MessageAdapter`：per-reply reducer，只累积文本，`AgentEnd` 时销毁 | Adapter + 生命周期单测 | 1.5 | R3 | P2-1 | E §P2 | INV-8 |
| P2-3 | 表达型工具 `send_hotel_cards`（返回值带 `shown` 摘要供模型续接） | 工具实现 | 1.5 | R3 | P0-5 | D §工具模块 | — |
| P2-4 | `onActing` middleware：取 `toolCalls()` 入参 → 服务端补全 → `withMetadataEntry`；**阻塞补全显式 `subscribeOn(boundedElastic)`** | middleware + BlockHound 单测 | 2.0 | R3 | P2-3 | E §P2 风险 | **INV-7** |
| P2-5 | 补全后的**完整卡片冻结落消息表**，重开会话不重查 | 落库路径 + 一致性测试 | 1.5 | R3 | P2-4, P1-5 | D3 | INV-5 |
| P2-6 | "一种富消息 = 一个 tool + 一个 middleware" 接入模板 + 文档 | 模板工程 + 接入指引 | 1.0 | R3 | P2-5 | D | — |
| P2-7 | 前端配合：卡片 UI 标注数据时间（"数据截至 …"） | 前端联调 | 0.5 | R3 | P2-5 | D3 | — |

**G2 门禁**：酒店推荐场景——推荐理由 + 三张卡片的消息组件 + 追问，**按正确顺序到达**；
关闭页面重开，卡片内容与首次生成时**逐字节一致**；BlockHound 在 P2 集成测试中零告警（提前到 P2，不等 P6）。

---

### P3　多节点正确性　18 人日 · W4–W6 · R1 主导 ★

> **本阶段拆两个子里程碑**，避免"三周后一次性验收"变成三周后才发现走偏。
> **P3a（W4–W5 中）= 机制建成**：Lua、消费组、心跳、回收、清理全部可跑，混沌台就绪。
> **P3b（W5 中–W6）= 正确性验证**：混沌测试跑通四类故障注入。

| ID | 任务 | 交付物 | 人日 | 负责 | 里程碑 | 依据 | 不变量 |
|---|---|---|---|---|---|---|---|
| P3-1 | 投递终态：两条命令**都成功才返回 202**，任一失败 5xx；客户端同 `instructionId` 重试写入协议文档 | 投递路径 + 故障注入测试 | 1.0 | R2 | P3a | C | **INV-1** |
| P3-2 | **摘牌 Lua**：校验持有 → 确认 inbox 已抽干 → `DEL lease`；启动 `SCRIPT LOAD`，运行期 `EVALSHA`；**单独成文件并注释为何必须原子** | `unlease.lua` + testcontainers 单测 | 2.5 | R1 | P3a | E §P3 | **INV-2** |
| P3-3 | 消费组初始化：`XGROUP CREATE ready workers 0 MKSTREAM`，吞掉 `BUSYGROUP` | 启动钩子 | 0.5 | R1 | P3a | E §P3 | — |
| P3-4 | consumer 名 = pod 名（**不加时间戳后缀**）+ 注册/生命周期管理 | ConsumerRegistry | 0.5 | R1 | P3a | E §P3 | INV-2d |
| P3-5 | **持牌期心跳**：与 lease 续租同频（10s）`XCLAIM ready workers <自己> 0 <令牌ID> JUSTID` | 心跳任务 | 1.0 | R1 | P3a | E §P3 | **INV-2c** |
| P3-6 | **`XAUTOCLAIM` 回收**：启动跑一次 + 每 30s；`MIN-IDLE-TIME 60000`（**不能填 0**）；循环游标至 `0-0`；任意 pod 都跑，幂等，不选主 | 回收任务 + 单测 | 2.0 | R1 | P3a | E §P3 | **INV-2b** |
| P3-7 | 死 consumer 清理：`XINFO CONSUMERS` → idle > 1h **且 pending = 0** → `XGROUP DELCONSUMER`；**排在 `XAUTOCLAIM` 之后** | 清理任务 + 顺序断言测试 | 1.0 | R1 | P3a | E §P3 | **INV-2d** |
| P3-8 | `ready` 裁剪 `MAXLEN ~ 100000` + 裁剪安全说明（**`XTRIM` 不看 PEL**） | 配置 + 文档注释 | 0.25 | R1 | P3a | E §P3 | INV-2b |
| P3-9 | **执行顺序固定**：抽干 → 摘牌(Lua) → `XACK` 令牌；续租任务在 `doFinally` 取消 | Worker 收尾路径 | 1.0 | R1 | P3a | E §P3 | **INV-4** |
| P3-10 | lease 续租每 10s + **续租失败立即中止 turn 并停止写 outbox** | 续租器 + 中止路径 | 1.5 | R1 | P3a | E §P3 | **INV-3 / INV-4** |
| P3-11 | 在飞任务上限 + **认领数量 = 空闲槽位数**（满则停止消费） | 背压控制 | 1.0 | R2 | P3a | D | — |
| P3-12 | 双游标（`msg` / `ctrl`）+ `instructionId` 幂等（对照上下文最后一条 USER 消息去重）；**cursor 不设 TTL，随 session 显式清理** | 游标管理 + 幂等测试 | 2.0 | R2 | P3a | C §陷阱 | INV-1 |
| P3-13 | `AgentStateStore` 走支持 CAS 的后端，冲突失败重试 | CAS 装配 | 1.0 | R2 | P3a | E §P3 | — |
| P3-14 | **混沌测试台**：随机 kill pod / Redis 抖动注入 / 长 GC 模拟 / 网络分区 | `agent-it` 混沌套件 | 2.0 | R2 | P3a（W4 起） | E §P3 验收 | 全部 |
| P3-15 | 混沌验收执行与缺陷修复 | 验收报告 | 0.75 | 全员 | P3b | E §P3 | 全部 |

**G3 门禁**（**本计划最硬的一道**）：
持续对话下随机 kill pod、注入 Redis 抖动、模拟长 GC，要求 **无消息丢失、无重复回复、无 session 永久卡死**；
优雅停机用户无感知；硬杀恢复时延 ≈ lease TTL（30s 内）。
四条不变量各有专属故障注入用例（见 §5.3），**缺一条不予放行**。

> **兜底分支**：若 P0-1 结论是 Lua 被禁，P3-2 替换为"每 60s 扫描 inbox 非空且无 lease 的 session 重新入队"，
> P3 估算降到 11 人日，G3 的恢复时延指标从 30s 放宽到 90s，并在协议文档中标注这一 SLA。

---

### P4　断线重连与一致性　5 人日 · W7 · R2 主导

> 规划已说明：D2 + 全量重放两个决定叠加后，服务端续传定位逻辑整块删除，本阶段从 2 人周降为 1 人周。
> **本阶段的正确性主要在客户端**，服务端很薄。

| ID | 任务 | 交付物 | 人日 | 负责 | 前置 | 依据 | 不变量 |
|---|---|---|---|---|---|---|---|
| P4-1 | SSE 建连从 outbox 窗口起点全量重放，**不解析任何客户端位置参数**（含 `Last-Event-ID`） | 发消息模块收敛 | 0.5 | R2 | P1-6 | B | — |
| P4-2 | **`turnStartId` 保护**：turn 进行中不允许裁剪 outbox | 裁剪守卫 + 长 turn 测试 | 1.0 | R2 | P3-9 | E §P4 | **INV-6** |
| P4-3 | 历史拉取 `GET /sessions/{sid}/messages?since=<msgSeq>&limit=`，直读消息表 | 接口 + 分页测试 | 1.0 | R2 | P0-7 | E §P4 | — |
| P4-4 | outbox 保留窗口收敛到 5–10 分钟（长期历史由消息表兜底） | 裁剪策略配置 | 0.5 | R2 | P4-2 | G | INV-6 |
| P4-5 | 消息表加 `superseded` 列；拉取接口过滤被重跑作废的 `replyId` | 迁移脚本 + 过滤逻辑 | 0.5 | R2 | P4-3 | E §P4 | — |
| P4-6 | **客户端三规则参考实现**（丢弃 / 追加 / 清空→拉取→重建）+ 可测试单元 | 参考实现 + 单测套件（交付前端） | 1.0 | R3 | P4-3 | B §重连协议 | **INV-10** |
| P4-7 | 断网重连验收：turn 中反复断网、超窗离线重连 | 验收报告 | 0.5 | R2+R3 | 全部 | E §P4 | — |

**G4 门禁**：turn 进行中反复断网重连，最终文本与不断网时**逐字节相同**；
离线超过 outbox 窗口后重连能从消息表无缝续上，**不出现空洞也不出现重复**；
**首次打开会话与空窗恢复走的是同一条代码路径**（代码评审确认，不是两个分支）。

> P4-6 的三规则若前端不实现，服务端做得再对也没用——"清空"后面必须跟着"拉取"，
> 只清空会让用户看到空白页。所以本计划把参考实现和单测**作为服务端交付物**交给前端，而不是口头约定。

---

### P5　控制通道与打断　10 人日 · W7–W8 · R1+R2

| ID | 任务 | 交付物 | 人日 | 负责 | 前置 | 依据 | 不变量 |
|---|---|---|---|---|---|---|---|
| P5-1 | **ctrl 状态 Lua**：合并 patch → `XADD ctrl-stream` → **水位 `ctrlId` 写回 state**，三步原子 | `ctrl-state.lua` + testcontainers 单测 | 2.0 | R1 | P3-2 | B §控制通道 | **INV-11** |
| P5-2 | control SSE 建连三步：`GET state`（含 `ctrlId`）→ 下发快照 → `XREAD` 从水位之后重放 | ctrl SSE 端点 | 1.5 | R2 | P5-1 | B | INV-11 |
| P5-3 | ctrl-stream **非阻塞轮询**（200ms），**禁止 `XREAD BLOCK`**；每条 SSE 各自轮询 | 轮询器 + 连接数压测 | 1.0 | R2 | P5-2 | E §P5 | **INV-12** |
| P5-4 | ctrl-stream `MAXLEN ~ 200` + TTL，与 state key 同生命周期 | 配置 | 0.25 | R1 | P5-1 | C | — |
| P5-5 | 状态帧字段：`turnActive`/`inputAllowed`/`activeReplyId`/`phase`/`stopping`/`supersededReplyId` | 协议扩展 | 0.75 | R2 | P5-1 | E §P5 | — |
| P5-6 | inbox 指令分类：`IMMEDIATE`（打断）/ `QUEUED`（HITL 恢复、状态类） | 指令模型 + 分派 | 1.0 | R1 | P3-12 | E §P5 | — |
| P5-7 | **pod 级 200ms 轮询器**扫本 pod 持牌 session 的 inbox，只捞 `IMMEDIATE` | 轮询器 | 1.5 | R1 | P5-6 | E §P5 | INV-12 |
| P5-8 | **跨节点中断复用 inbox**（不开 pub/sub）：任意 pod 写 cancel（`IMMEDIATE` + `targetReplyId`），持牌 pod 扫到后 `agent.interrupt()` | 中断链路 | 1.0 | R1 | P5-7 | G | — |
| P5-9 | `targetReplyId` 作用域校验：目标 turn 已不存在则**丢弃并推进游标** | 校验逻辑 + 重跑测试 | 0.5 | R1 | P5-8 | E §P5 | — |
| P5-10 | turn 重跑清理：接管方写 `supersededReplyId`，消息表对应行标记 `superseded` | 清理路径 | 0.5 | R2 | P4-5, P5-5 | E §P5 | — |

**G5 门禁**：turn 进行中点停止，**1 秒内 UI 进入"正在停止"**，已生成部分保留；
kill 执行节点后重跑，**旧的停止指令不会打断新 turn**；
**反复断开重连 control SSE，状态不出现翻转**（不出现先 `turnActive:false` 又变回 `true`）。

---

### P6　容量与降级　10 人日 · W9 · 全员

| ID | 任务 | 交付物 | 人日 | 负责 | 前置 | 依据 | 不变量 |
|---|---|---|---|---|---|---|---|
| P6-1 | **A 准入**：ready 队列深度阈值 → 超限 `503 + Retry-After`（无租户配额） | 准入过滤器 | 1.5 | R2 | P3-11 | E §P6 | — |
| P6-2 | **B 执行并发**：`flatMap(…, N)`，N 按 LLM 配额倒推（**必须显式设置，默认 256 是坑**） | 并发配置 | 1.0 | R1 | P3-11 | E §P6 | INV-8 |
| P6-3 | 429 处理：抖动退避 `jitter(0.5)`（**无抖动的退避比不退避更糟**） | 退避策略 + 单测 | 1.0 | R1 | — | E §P6 | — |
| P6-4 | **首轮 429 退回队列**：未产出任何用户可见内容时，不推进游标、摘牌走人 | 降级路径 | 1.5 | R1 | P3-9 | E §P6 | INV-4 |
| P6-5 | 模型降级：连续 429 切换独立配额池小模型 | 降级策略 | 1.5 | R3 | P6-3 | E §P6 | — |
| P6-6 | 429 率反馈到 A：>20% 收紧准入，<5% 放开 | 自适应控制器 | 1.0 | R2 | P6-1, P6-3 | E §P6 | — |
| P6-7 | BlockHound 接入**全部**集成测试（P2 已试点，此处全量） | CI 集成 | 0.5 | R3 | P2-4 | E §P6 | **INV-7** |
| P6-8 | 可观测：队列深度 / `inFlight` 水位 / 429 率 / 牌子持有时长 + 告警（见 §6） | Micrometer 指标 + 面板 + 告警 | 1.5 | R2 | — | E §P6 | — |
| P6-9 | 压测至配额上限并调参 | 压测报告 | 0.5 | 全员 | 全部 | E §P6 | — |

**G6 门禁**：压测至配额上限——**无 turn 永久卡死**；超载用户收到 503 而不是超长等待；
BlockHound 在集成测试中**零告警**。

---

### ∥　并行轨道：Skill 管理　2.5 人日 · W6 · R3

P1 之后任意时间可做，不阻塞主线；本计划排在 W6（R3 在 P2 与前端联调之间的空档）。

| ID | 任务 | 人日 | 依据 |
|---|---|---|---|
| SK-1 | `JdbcSkillRepository`：`name` / `description` 单独成列，正文与资源分开存 | 1.0 | E §∥ |
| SK-2 | **版本缓存必做**：`MAX(updated_at)` + 行数当版本号，变了才重载（`getAllSkills()` 是每轮推理热路径） | 1.0 | E §∥ |
| SK-3 | 生产 `writeable(false)`；若开 `enableSkillManageTool` **必须**配 `enableSkillPromotionGate`，严禁 `autoPromote=true` | 0.25 | E §∥ |
| SK-4 | DB 故障降级返回上次快照，不抛异常（skill 缺失应降级，不该让 turn 挂掉） | 0.25 | E §∥ |

---

## 4. 阶段门禁（Definition of Done）

每个门禁都是**一次 30 分钟的走查会**，不过则该阶段不结束。全阶段通用项 + 阶段专属项（见 §3 各阶段门禁）。

**全阶段通用（每个 G 都要过）**：

- [ ] 单元测试覆盖率 ≥ 80%，新增代码无覆盖率倒退
- [ ] `code-reviewer` 与 `security-reviewer` 走查通过，CRITICAL / HIGH 清零
- [ ] 本阶段涉及的不变量各有一条**会失败的测试**（先看到它红，再看到它绿）
- [ ] 无阻塞调用泄漏到事件循环（BlockHound，P2 起生效）
- [ ] 无硬编码 key 字面量（ArchUnit 守护）
- [ ] 协议若有变更，已同步前端并更新协议文档版本号

---

## 5. 测试策略

### 5.1 分层

| 层 | 范围 | 工具 | 归属阶段 |
|---|---|---|---|
| 单元 | `render()` 纯函数、msgSeq 分配、客户端三规则、patch 合并、退避抖动 | JUnit 5 + AssertJ | 全程 |
| 集成 | Redis 脚本、消费组、游标、SSE 端到端 | **testcontainers（Redis 7.0，与生产同版本）** | P1 起 |
| 契约 | 键命名快照测试、协议 v1 序列化快照 | 快照断言 | P0 起 |
| 混沌 | kill pod / Redis 抖动 / 长 GC / 网络分区 | 自研注入 + toxiproxy | **P3** |
| 阻塞检测 | 事件循环线程 | BlockHound | P2 试点，P6 全量 |
| 压测 | 配额上限、SSE 连接数上限 | Gatling / k6 | P6 |

### 5.2 Lua 脚本测试是硬要求

两个脚本（摘牌、ctrl 状态）**必须用 testcontainers 起真 Redis 写单测**，不接受 mock。
理由：这两个脚本的价值 100% 在于原子性，而原子性是 mock 唯一无法验证的性质。

### 5.3 不变量 → 测试用例映射

**这张表是 P3/P5 门禁的检查清单。**每一行必须有一个能跑、能红的测试。

| 不变量 | 测试用例 | 注入方式 | 阶段 |
|---|---|---|---|
| INV-1 顺序 inbox 先 ready 后 | `deliveryOrder_readyBeforeInbox_losesWakeup` | 反序执行，断言摘牌脚本漏检新消息 | P3 |
| INV-2 摘牌与"inbox 已空"同脚本 | `unlease_messageArrivesDuringRelease_notOrphaned` | 释放瞬间并发 `XADD` | P3 |
| INV-2b `XAUTOCLAIM` 必跑 | `pel_withoutAutoclaim_staysPendingForever` | 关掉回收任务，断言令牌永久滞留 | P3 |
| INV-2c 持牌期 `XCLAIM JUSTID` | `longTurn_withoutHeartbeat_getsReclaimed` | 模拟超过 MIN-IDLE 的长 turn | P3 |
| INV-2d `DELCONSUMER` 需 pending=0 且在回收后 | `delConsumer_withPending_workVanishes` | 有 pending 时删除，断言条目消失 | P3 |
| INV-3 lease 值每次唯一 | `lease_samePodTwoWorkers_noCrossRelease` | 同 pod 两 worker 竞争 | P3 |
| INV-4 抽干→摘牌→交差 | `crash_betweenAckAndUnlease_leavesNoTrace` | ACK 后 kill | P3 |
| INV-5 先落库后 XADD | `xaddFails_messageStillInDb_clientHealsGap` | 注入 `XADD` 失败 | P1 |
| INV-6 `turnStartId` 保护 | `longTurn_outboxTrimmed_createsHole` | turn 中触发裁剪 | P4 |
| INV-7 middleware 阻塞需 offload | BlockHound 断言（无专属用例，全局生效） | — | P2 |
| INV-8 事件管道 `concatMap` | `largeBatch_withFlatMap_arrivesOutOfOrder` | 放大批次 | P1 |
| INV-9 hash tag 与 256 分片 | ArchUnit + `keyNamespace_shardCount_isFrozen` | 常量快照 | P0 |
| INV-10 msgSeq 单调无洞 | `msgSeq_interleavedSessions_noGaps` | 多 session 交错 | P0 |
| INV-11 ctrlId 与 XADD 同脚本 | `ctrl_reconnect_noStateFlip` | 反复重连 + 并发变更 | P5 |
| INV-12 ctrl 用非阻塞轮询 | `sseConnections_scaleUp_redisConnectionsFlat` | 拉到 1000 条 SSE 观察连接数 | P5 |

---

## 6. 可观测性交付清单

P6-8 的具体内容，但**指标埋点随各阶段同步加**，不要攒到最后。

| 指标 | 类型 | 告警阈值 | 说明 |
|---|---|---|---|
| `ready` 队列深度 | Gauge | > 准入阈值 80% | 驱动 A 准入 |
| `inFlight` turn 数 | Gauge | > N 的 90% | 驱动 B 并发 |
| 429 率 | Rate | > 20% 收紧 / < 5% 放开 | 反馈到准入（P6-6） |
| lease 持有时长 P99 | Histogram | > 5 min | 卡死的早期信号 |
| PEL 深度 / 最老条目 idle | Gauge | idle > 5 min | **INV-2b 失效的唯一可观测信号** |
| outbox 滞留条数 | Gauge | 持续增长 | 消费端异常 |
| msgSeq 空洞检测（客户端上报） | Counter | > 0 持续 | INV-5/10 失效 |
| SSE 连接数 vs Redis 连接数 | Gauge 对比 | 二者线性相关即报警 | **INV-12 失效的信号** |
| 续租失败次数 | Counter | > 0 | 网络或 Redis 抖动 |

> PEL 深度这条最容易漏。Redis **没有** PEL 自动回队机制，`XAUTOCLAIM` 停跑时
> Redis 视角一切正常、没有任何错误日志——只有这个指标能看见。

---

## 7. 风险登记册

| # | 风险 | 概率 | 影响 | 早期信号 | 缓解 | 兜底 |
|---|---|---|---|---|---|---|
| R-1 | 云 Redis 禁用 Lua 或脚本内 `XADD *` | 低 | **极高**（P3 整体重做） | P0-1 实测 | W1 周一即测，最先做 | 60s 扫描兜底方案，P3 降到 11 人日，恢复 SLA 放宽到 90s |
| R-2 | P3 复杂度集中，单点依赖 R1 | **高** | 高 | P3a 里程碑延期 | 拆 P3a/P3b 两个子里程碑；W4 起 R2 并行搭混沌台；关键脚本结对 | P3b 顺延一周，压缩 P6 的 P6-5 |
| R-3 | middleware 阻塞调用拖垮事件循环（INV-7） | 中 | 高 | 吞吐莫名掉到个位数 | BlockHound **提前到 P2** 而非 P6 | 单独 `boundedElastic` 池隔离补全逻辑 |
| R-4 | 前端未按三规则实现去重/空窗 | 中 | 高 | 用户报"消息重复/空白" | P4-6 交付参考实现 + 单测，不做口头约定 | 服务端临时加位置参数兼容（**技术债，需记录**） |
| R-5 | 多 pod 记忆按 pod 分叉 | 中 | 中 | 跨会话记忆丢失 | 采用方案 B（`RemoteFilesystemSpec` + Redis） | 退方案 A，关长期记忆 |
| R-6 | 协议 v1 在 P2/P5 被迫破坏性变更 | 中 | 中 | 前端反复返工 | P0-8 预留 `type` 扩展位与能力协商 | 协议版本号 + 双版本并行一个迭代 |
| R-7 | outbox 窗口/裁剪配置错误导致静默丢数据 | 低 | 高 | 续传出现空洞 | INV-6 专属测试 + 裁剪守卫 | 消息表兜底（真相源在 DB） |
| R-8 | 覆盖率门禁拖慢 P3 进度 | 中 | 低 | CI 频繁红 | P3 的混沌测试计入覆盖率 | 门禁按模块设差异化阈值 |

---

## 8. 分工与协作

### 8.1 角色主责

| 角色 | 主责模块 | 主投入阶段 |
|---|---|---|
| **R1** 分布式/Redis | `agent-task`、`agent-keys`、Lua 脚本 | P3（主）、P5（主）、P1 |
| **R2** 通信层/协议 | `agent-comm`、`agent-protocol`、`agent-store` | P1（主）、P4（主）、P6 |
| **R3** Agent/工具/消息 | `agent-agent`、OutboxWriter、render/adapter | P2（主）、∥Skill、前端联调 |

### 8.2 P2 ‖ P3 并行的隔离约定（W4–W5）

这两个阶段同时进行，**必须避免改同一个文件**：

| 轨道 | 允许改 | 禁止碰 |
|---|---|---|
| P2（R3） | `agent-agent/**`、`OutboxWriter` 的 render 分支 | `agent-task/**`、Lua 脚本、游标与 lease |
| P3（R1+R2） | `agent-task/**`、`agent-keys/**`、Lua、游标 | `agent-agent/**`、render/adapter |

交界点只有一处：**Worker 的事件分流**。约定 W4 周一先由 R1 把分流接口定死（三条分支：状态事件 / 消息工具 / 全量日志），
之后两边各改各的实现，不动接口。

### 8.3 例会

- **每日站会 15 分钟**：只说阻塞项，不过进度
- **每周五门禁走查**：对照 §4 清单逐条过，不过则该阶段不关闭
- **P3 期间加一次周三中盘复盘**：这是唯一一个复杂度足以在一周内跑偏的阶段

---

## 9. 变更流程

规划 G 节的决策、C 节的键名、F 节的不变量属于**冻结项**。要改，走这三步：

1. 在 `开发规划.md` 的对应表格里改，写明新决定与后果（规划是真相源，计划跟随）
2. 评估受影响的任务卡与门禁，更新本文的估算与排期
3. **分片数 256 与已上线的键名不接受变更** —— 改这两项等于数据迁移，按新项目立项

---

## 10. 执行记录

> 随进度更新。只记结论与它改变了什么，不记流水账。

### P0-1　Redis 环境实测　✅ 已完成

`./bin/agent doctor` 对本地 Redis 7.0.15 实测，**八项全通过**：

| 项 | 结果 |
|---|---|
| 引擎版本 | 7.0.15 |
| 架构 | 标准版（主从），Lua 无槽约束 |
| `EVAL` / `SCRIPT LOAD` / `EVALSHA` | 可用 |
| **脚本内 `XADD ... *`** | **可用**，实测生成 stream ID |
| `XAUTOCLAIM` | 可用 |
| `XTRIM MINID` | 可用 |

**结论：P3 按原设计推进，不需要兜底扫描方案。INV-11（ctrl 水位与 XADD 原子产生）成立。**
§7 的风险 **R-1 关闭**。

### P0-7　msgSeq 分配　✅ 已完成

改为 PostgreSQL 实现（原计划是 Redis `INCR`）。`INSERT ... ON CONFLICT DO UPDATE ... RETURNING`
单语句完成，行锁内原子。真库并发测试：8 线程各分配 10 次，排序后为 1..80 连续无洞。
消息表主键 `(session_id, msg_seq)` 另外从数据库层面强制了唯一性 —— **INV-10 的两半都有了着落**。

### P2　消息渲染　✅ 已完成，G2 通过

新增模块 `agent-tools`。**一种富消息 = 一个表达型工具 + 一个补全 middleware + 一个渲染器**，
用 `ToolBundle` 绑定交付，接入时不会漏掉其中一环。接入指引见
[`docs/富消息接入.md`](./docs/富消息接入.md)。

| 任务 | 落地 |
|---|---|
| P2-1 渲染纯函数 | `HotelCardRenderer implements RichMessageRenderer`，7 条表驱动单测 |
| P2-2 per-reply reducer | **不需要状态**：blockKey 取自模型的 messageId，`EventMapper` 保持无状态。顺序由 `concatMap` 保证，与文本的交错由客户端 `Transcript` 处理 |
| P2-3 表达型工具 | `send_hotel_cards`，返回值带 `shown` 摘要供模型续接 |
| P2-4 onActing 补全 | `HotelEnrichmentMiddleware`，**BlockHound 守 INV-7 且附反证** |
| P2-5 卡片冻结落库 | 实测重开会话卡片 payload 逐字节一致，且不重查业务源 |
| P2-6 接入模板 | `docs/富消息接入.md` |
| P2-7 UI 标注数据时间 | 终端渲染 `└ 数据截至 …` |

**修掉的一个真 bug（实跑真实模型才暴露）**：`ToolUseBlock` 有两份入参 ——
结构化的 `input` 和模型原始的 JSON 串 `content`，**参数校验与实际绑定走的是 `content`**。
middleware 用三参构造器重建时把 `content` 置空，模型侧直接报
`argument "content" is null`，补全根本送不到工具。这一点在方法签名上完全看不出来。

**顺带修的 UX 缺陷**：查询工具返回的大段 JSON 原样打在屏幕上，把对话内容挤没了。
现在压成一行（认得出 `shown` / `count` 就用它们，否则截断），压缩只影响展示。

### P0-2　workspace 方案 B　✅ 已完成并验证

`RemoteFilesystemSpec` + PG `BaseStore` + `IsolationScope.USER`。
真实模型调用 `memory_save` 后，`MEMORY.md` 与 `memory/2026-08-24.md` 落在
`agents/agent/users/dev/...` 命名空间下（PG `agent_store_item` 表），本地磁盘没有副本。
**规划 A 节担心的"记忆按 pod 分叉"已解决。**

**边界**：这个 spec 只路由特定前缀（`MEMORY.md` / `memory/` / `skills/` / `plans/` /
`knowledge/` / `agents/<id>/sessions/` 等）。写在 workspace 根的任意文件仍落本地磁盘。

**一个安全决定**：文件系统工具改为默认关闭。实测模型可经 `../` 穿越出 workspace
列到宿主机根目录 —— D4 决定不用沙箱，那么这类工具就必须显式开启，与 shell 工具同等对待。

### 协议 v1.1　用户消息落库　✅ 已完成（走 §9 变更流程）

**变更**：`ClientMessage` 增加 `role`（USER / ASSISTANT / SYSTEM）与 `MessageType.TEXT`；
消息表增加 `msg_role`、`instruction_id` 两列与 `(session_id, instruction_id)` 唯一索引。

**决定**：用户消息由服务端落库后经流推回，**客户端不再本地回显**。
理由是本地回显与流重放是两套顺序来源，只有一套受 msgSeq 约束；
去掉之后一个会话只剩一个 seq 空间，多端顺序必然一致，空窗判定也不会被本地插入的行打乱。

**落地要点**：

- 幂等键写进数据库唯一索引，而不是只靠应用层查一次。客户端重试（INV-1）在库层面就写不进第二条
- **先查后分配**：先分配序号再发现是重试会烧掉一个序号，序列留洞，直接违反 INV-10。
  「查」与「插」之间的竞态在真实架构里不存在 —— lease 保证同一 session 同时只有一个 worker（INV-3）
- 命中幂等时**不起第二轮**，回执带回原来的 `replyId`，否则客户端会去等一个不存在的 turn
- `markSuperseded` 加 `msg_role <> 'USER'`：重跑作废助手输出，用户说过的话不跟着消失
- 由 **Worker 侧**落用户消息，不是收消息模块。理由是幂等机制（P3-12）本来就在 Worker，
  且消息表与 outbox 保持单一写入者。代价是队列积压时用户的话要等 worker 摘牌才出现，
  这段由控制通道的 `phase: QUEUED` 覆盖

**已用真实模型验证**：`msg_seq=1` 为 `USER/TEXT` 带 `instruction_id`，
之后才是助手的 `TEXT_DELTA`；重开会话时历史里能看到自己问过的话。

### P0-5　HarnessAgent 单例　✅ 已完成

`io.agentscope:agentscope-harness:2.0.2`，装配含 PG 版 `AgentStateStore` 与 `BaseStore`、
`RemoteFilesystemSpec` + `IsolationScope.USER`、不引入沙箱。
端到端验证到模型调用为止（凭据缺失时正确返回 401 并落库为 ERROR 消息）。

**已用真实模型（Kimi `kimi-for-coding`）端到端验证**：单轮回复 35 条 TEXT_DELTA + 1 条
TEXT_END，序号 1–36 连续无洞、共享同一 blockId、无重复正文；工具调用链
（`write_file` → `read_file` → `memory_save`）映射正确；进程重启后同一
`(userId, sessionId)` 上下文延续 —— **G0 门禁的核心项达成**。

**发现一处需要修订计划的地方**：P3-13 写的是"`AgentStateStore` 走支持 CAS 的后端，冲突失败重试"，
但上游接口 `save(userId, sessionId, key, State)` 不接收版本号，**表达不了 CAS**。
防双跑的实际防线只能是 lease（INV-3）。存储层若要再加一道，用 `PostgresAgentStateStore.saveIfVersion`，
它不在上游接口里。P3 评审时需要就此调整任务描述。

---

## 11. 首周行动清单（W1 Day 1）

按优先级排，前两条当天必须启动：

1. **R1**：申请阿里云 Redis 7.0 标准版测试实例，跑 `EVAL` / `EVALSHA` / 脚本内 `XADD ... *` 三项实测 → 当天出初步结论（P0-1）
2. **R3**：组织 workspace 方案决策会（方案 A vs B），当天定 → 解锁 P0-5（P0-2）
3. **R2**：拉起 8 模块 Maven 骨架 + CI + ArchUnit 守护测试（P0-3）
4. **全员**：通读 `开发规划.md` F 节 12 条不变量，逐条确认理解一致——这 12 条会以 code review checklist 的形式贯穿整个项目
